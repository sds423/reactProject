import React from 'react';

let Sample=(param)=>{

	return(
		<div>
		    <h2 style={{color:"blue"}}>My name is {param.name} and key is {param.index}</h2>
		</div>
		)
}

export default Sample;